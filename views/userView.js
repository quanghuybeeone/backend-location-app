const userView = {
    renderUsers: (users) => {
      // Xử lý mã HTML để hiển thị người dùng
      // Ví dụ: res.send(`<ul>${users.map(user => `<li>${user.name}</li>`).join('')}</ul>`);
    },
  };
  
  module.exports = userView;